kind: Deployment
apiVersion: apps/v1
metadata:
  name: rm-api
  namespace: resource-manager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rm-api
  template:
    metadata:
      labels:
        app: rm-api
    spec:
      containers:
        - name: rm-api
          image: {{ACR}}.azurecr.io/rm-api:{{TAG_ID}}
          resources:
            limits:
              cpu: 8
              memory: 8Gi
            requests:
              cpu: 6
              memory: 8Gi
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: sp-cert-volume
              mountPath: /mnt/certificates
              readOnly: true
          envFrom:
            - secretRef:
                name: rm-all-secret
          env:
            - name: AZURE_CLIENT_CERT_PATH
              value: /mnt/certificates/certificate.pem
      volumes:
        - name: sp-cert-volume
          secret:
            secretName: rm-sp-cert-secret
            optional: true
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler